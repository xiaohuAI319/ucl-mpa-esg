# 强制使用 pnpm 作为包管理器
# 如果检测到使用 npm 或 yarn，将抛出错误

# 防止使用 npm
engine-strict=true

# 指定包管理器（package.json 中需配置 "packageManager" 字段）
# 如果用户尝试使用 npm install，会提示错误

# pnpm 配置
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true

# 锁定文件
lockfile=true

# 安全设置
audit-level=moderate
